<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
<h4>Title</h4>
<%= f.text_field :title %>
<h4>Opinion</h4>
<%= f.text_field :body %><br>
<%= f.submit 'Update Book' %><br>
<%= link_to "Show", book_path(@book) %>
<!-- back(book一覧へ) -->

<% end %>